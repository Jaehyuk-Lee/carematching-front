# 이 환경변수들은 src/config/config.js에서 사용합니다.

# 게이트웨이 기본 주소 (프론트가 백엔드 서비스가 아닌 게이트웨이로 요청)
REACT_APP_API_URL=http://localhost:8080

# 전역 API 설정 (선택적 재정의)
REACT_APP_API_VERSION=v2

# 게이트웨이 라우트에서 사용하는 서비스별 prefix 및 버전
REACT_APP_PLATFORM_PREFIX=platform
REACT_APP_PLATFORM_VERSION=v2

REACT_APP_CHAT_PREFIX=chat
REACT_APP_CHAT_VERSION=v1
